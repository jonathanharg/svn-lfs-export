# SVN LFS Export

## Build
Configure
```
cmake -S . -B ./build
```
Alternatively build with a generator, e.g. `cmake -S . -B ./build -G Ninja` or `cmake -S . -B ./build -G Xcode`

Build
```
cmake --build ./build
```
